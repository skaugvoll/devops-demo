description: "Parses the log correlation IDs out of the raw plain-text log"
processors:
  - grok:
    field: "message"
    patterns:
      - "%{GREEDYDATA:message} | transaction.id=%{DATA:transaction.id} trace.id=%{DATA:trace.id} span.id=%{DATA:span.id} | %{GREEDYDATA:response.time}"
